"""Пакет с ядром pipeline контент-плана Kaiten.

Модули:
    config        — загрузка config/*.yaml, пути, даты цикла
    warnings_log  — сбор предупреждений (журнал), не роняющий pipeline
    io_utils      — чтение/запись CSV (utf-8-sig), безопасные хелперы
    schema        — канонические поля инициатив и размещений (+ рус. подписи)
    text_utils    — нормализация текста, ключевые слова, fuzzy
    taxonomy      — эвристическая классификация (кластер, роль, воронка, тип)
    legacy        — разбор Excel-книги контент-плана в единую схему
    signals       — загрузка сигналов (шаблоны CSV/MD + адаптеры Roistat/Метрики)
    dedupe        — поиск дублей, каннибализации, переносов, дистрибуции
    scoring       — оценка тем по config/scoring.yaml с обоснованием
    portfolio     — сборка портфеля, недели, проверка мощности
    excelout      — экспорт итогового .xlsx (11 листов, форматирование)

Скрипты в каталоге scripts/ — тонкие CLI-обёртки над этим пакетом.
"""

__version__ = "0.2.0"
