# Зависимости pipeline контент-плана Kaiten.
# Установка:  python -m pip install -r requirements.txt
#
# Версии зафиксированы «не ниже», чтобы не ломать совместимость,
# но и не устаревать. Проверено на Python 3.12.

openpyxl>=3.1,<4          # чтение и запись .xlsx (без потери форматирования)
pandas>=2.0               # табличная обработка (опционально, ускоряет агрегации)
PyYAML>=6.0               # чтение конфигов config/*.yaml
rapidfuzz>=3.0            # нечёткое сравнение тем (fuzzy matching дублей)
python-dateutil>=2.8      # разбор дат из разных форматов
